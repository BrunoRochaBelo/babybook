# Quando true (default em dev), usa MSW + dados mockados (persistidos no localStorage)
VITE_ENABLE_MSW=true

# Quando VITE_ENABLE_MSW=false, este base URL é usado para chamar o backend real
# Ex.: http://localhost:8000/api
VITE_API_BASE_URL=

# Base do link de divulgação do afiliado (onde o público entra). Exemplos:
# - Landing: http://localhost:5174
# - B2C app: http://localhost:5173
# Se não definido, o app faz fallback para window.location.origin (dev).
VITE_REFERRAL_LINK_BASE_URL=

# (Opcional) Guardrail do bridge dev (iframe) que grava vendas no localStorage.
# Lista separada por vírgula de ORIGINS permitidos como referrer.
VITE_AFFILIATE_BRIDGE_ALLOWED_REFERRERS=http://localhost:5173
